[project]
name = "fixture-cli"
version = "0.0.0"
requires-python = ">=3.11"
urls = {homepage = "https://example.invalid/notes"}

[project.scripts]
notes = "notes.cli:main"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
