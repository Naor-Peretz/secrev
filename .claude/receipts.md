# M0 receipts

One line per completed task: id | commit | what was actually asserted.

TASK-000 | fcd52ee | commits=1, worktree clean, gate exit 0 (no test file — baseline snapshot, no behaviour to test)
TASK-001 | dc46a54 | 16/16 green on unmodified harness; 2 fail + exit 1 when a guard block is defeated; guard restored byte-identical
TASK-002 | 67b8d94 | 18/18 green; malformed payload now refuses (was rc=5, undefined); exit 1 when the new H-1 path is defeated; guard restored byte-identical
TASK-002B | b94156e | 19/19 green; gate reports the stage; defeating a guard gives GATE exit 1 (was 0); guard restored byte-identical
TASK-003 | 65241ae | 34/34 green; brief §1 heredoc case refused with reason; cat/grep/head/git-diff of same path permitted; no false positives on transcripts|descriptions|mysrc
TASK-007 | 88d3ed0 | 42/42 green; 0 hooks invoke jq (6 mention it in comments); scope+spec malformed payload now exits 2 (was 5); defeating either H-1 path turns the gate red
TASK-008 | 0e222d6 | 47/47 green; eval into scripts/self_check.py blocked; patterns/ correctly exempt from self-application, covered by scope; removing scripts/ from paths.sh turns the gate red
TASK-009 | bfbaef4 | 52/52 green; relative src/secrev and scripts paths now guarded; over-match measured (transcripts, descripts, foosrc all refused; mypatterns and docs not); re-anchoring turns the gate red
TASK-010 | 3b48c25 | 57/57 green; M9+src exits 2, M9+README untouched, missing MILESTONE exits 2, M1 unchanged; both the refusal and the ordering fail the gate when defeated
TASK-011 | 74404f1 | 62/62 green; session-start reports M0 + BRIEF_M0.md; M0 permits scripts/ refuses src+patterns; M9 and missing marker still refuse; M0 fall-through turns the gate red
TASK-012 | f9e3882 | 64/64 green; no live agent restates STACK.md; removed copy was stale on 3 counts (claimed mypy as a recorded dep, predated §8 H-1..H-8, predated the jq decision); restating one line turns the gate red
TASK-013 | 3a779cd | 68/68 green; CLAUDE.md status table + hook table current, README hook count mechanical (11 == 11); 8 stale claims asserted absent; re-adding one, or adding a 12th hook, turns the gate red
TASK-014 | c6a9414 | 69/69 green; §8 now H-1..H-9; mypy in §2; uv no longer default and 3 remedy sites updated + asserted; one assertion retired because TASK-014 made its pinned claim true
TASK-005 | c460af4 | 72/72 green; gate now runs ruff+format+pytest for real (69 collected); 15 lint findings fixed not suppressed; pyproject dev deps unreachable by pip until moved; restoring the PATH fallback turns the gate red
TASK-009B | 3f2fe31 | 74/74 green; transcripts/descripts/foosrc no longer refused, relative+absolute still are; found and fixed 2 `cmd | head` status losses (async-check reported [ok] under a failing pytest; determinism-guard would have announced byte-identical over a failure)
TASK-004 | e528258 | 81/81 green; sh/python3 <script> permitted, bash and extension mismatch refused, -c refused; ; && || | newline substitution all refuse beside a protected path; read-only pipeline now refused (asserted cost); relaxing OPERATOR_CHARS turns the gate red
TASK-004B | 0800648 | 85/85 green; sed -i/rm/redirect into .claude refused, cat/grep/ls/sh of it permitted, Write untouched; removing .claude from PROTECTED turns the gate red (defeat verified by asserted anchor after a first attempt silently no-opped)
TASK-004C | 64f5e97 | 85/85 green; BRIEF §1 verification run LIVE through the Bash tool — heredoc to src/secrev/cli.py refused, sed -i refused; cost measured immediately (gate output can no longer be piped through grep); docs said "not wired" in 3 separate places
TASK-006 | a997d46 | 87/87 green; with .venv/bin/python moved aside the gate exits 2 naming ruff (same state gave exit 0 + "all gates pass" before); pytest branch no longer conflates "no tests" with "no pytest"; remedy string corrected — TASK-014's `pip install -e '.[dev]'` fails until src/ exists
TASK-012B | 060938a | 87/87 green; stack copies removed from disabled/planner.md and architect.md, both already stale (mypy as a recorded dep, no §8); H-7 assertion now covers disabled/; closes open question 6
TASK-016 | d937646 | 88/88 green; MILESTONE M0 -> M1, session-start reports M1 and finds BRIEF_M1.md; a new assertion refuses to let the marker move while any BRIEF_M0.md DoD box is unticked

# M1 receipts

Same form. "gate" is `sh scripts/check.sh` with every stage live; "harness" is `sh .claude/check.sh`.
Commit hashes are filled in at commit time; `uncommitted` means the work is on disk and gated but
not yet history.

PUBLICATION | 902f3bf | gate green; LICENSE (PolyForm Noncommercial 1.0.0, verbatim from upstream tag 1.0.0 + Required Notice), SECURITY.md, README.md, CONTRIBUTING.md, STACK.md §10; pyproject gains license/readme/authors — wheel build verified `License: PolyForm-Noncommercial-1.0.0` at metadata 2.4 with LICENSE packaged, and `pip install -e .` (the path CI's install-paths job asserts) still works; license_check.py's SELF comment and its printed line both claimed the project "carries none", now false and corrected
TASK-M1-001..004 | e053c26, 6c5879d | (recorded retroactively) determinism stage keyed on inventory.py; test_determinism.py red before the module; inventory.py + fixture tree; ids.py deriving from (relative_path, rule_id, window_sha256, ordinal) — the ledger's own acceptance text still named the superseded `line` formula and was corrected rather than ticked as written
DECISIONS-M1 | 9f3c4ed | gate + harness green; four owner decisions recorded with reasoning, not just applied — window_spec on candidate AND verification records plus a new FR-4.6 invalidation row (STACK.md §5, PRD FR-4.5/4.6/§7); default_severity_hint (PRD beat the brief on precedence); multi-element `layer` refused in M1 rather than silently flattened; path.traversal added as a ninth seed pattern with an explicit note that it does not reverse §1's M4 exclusion
TASK-M1-005 | 139998a | gate green, 22 catalog tests; unknown keys, bad flags, uncompilable regex, non-namespaced id, duplicate id, missing version, multi-layer all exit with the pattern id named; `multiline` refused by name with the reason; the safe-loader test was strengthened after review — it asserted only that a CatalogError was raised, which an unsafe loader would also have produced (the tag evaluates to 0 and the schema check then rejects it), so it passed against precisely the defect it was written to catch; now asserts the refusal happens at the YAML layer
TASK-M1-005B | 139998a | gate green; `python -m secrev sweep --help` now contains `--catalog`, so ci.yml's catalog job runs its assertion instead of taking the ::warning:: branch and exiting 0 having asserted nothing (H-1 in the job that exists to prove exit 2); verified live: malformed catalog exits 2 with `ci.deliberately_broken` on stderr, which is where that job reads it from
TASK-M1-006 | 139998a | gate green, 25 pattern tests; nine patterns, positive+negative fixture each, pairing asserted in BOTH directions so an added pattern fails and a renamed one leaves no orphan; the negative fixture earned its place immediately — net.bind_all was a bare `0\.0\.0\.0`, which matches inside `10.0.0.0/8`, caught before the rule had run against anything real; owasp omitted from 8 of 9 rather than guessed (§4: omit rather than force a bad fit); fixtures live in tests/fixtures/rules/ not .../patterns/, because the Bash guard's unanchored glob (H-5) reads the latter as the protected catalog
TASK-M1-007 | 139998a | gate green, 28 sweep tests; golden byte-comparison (35 records); two runs identical; adding a file ahead of another changes no existing id; an edit >20 lines above a match does not re-identify it; CRLF and LF produce the same ids while line numbers stay against the original; D-6 held for two rules on one line and two identical matches on one line; the non-ASCII fixture matched nothing so it never reached the golden — the §9 requirement was satisfied on paper only, and the fixture now carries a candidate; G-3 redaction asserted, and the test's own `ghp_`-shaped literal was flagged by gitleaks 4 times and replaced with a neutral one rather than allowlisted
TASK-M1-008 | 139998a | gate green, 15 recon tests + golden; determinism stage's "waiting on recon.py, sweep.py" is gone and the artifact half now runs — recon.json and hits.jsonl byte-identical, ids stable; no subprocess anywhere in the codebase, git identity read from .git/ directly (loose refs then packed-refs), degrading to ("directory", None) rather than guessing a version that would split one target's history across two workspace paths; the slug test found a real defect (café slugged to `caf-`) after being rewritten to build NFC/NFD explicitly instead of comparing a literal to itself
TASK-M1-009 | 139998a | gate green, 15 CLI tests; exit 0/2/3 asserted apart, and sweep finding 35 candidates still exits 0 because exit 1 means "unresolved remain" and in M1 every candidate is unresolved by definition; stdout parses as JSONL with the workspace line on stderr; G-4 refusal verified live and asserted, including that no directory is created; run.json is the only artifact carrying a timestamp; the workspace file and stdout are byte-identical
TASK-M1-012 | 139998a | gate green; STACK.md §5's exclusion list extended with .venv-audit and the tool caches (exact names, not a `.venv*`/`*_cache` pattern — P3); recon of this repository goes from 1952 files / 703,763 lines / 1416 Python files to 137 / 16,348 / 38; resolved rather than raised because §5 and §2.2 are both in STACK.md and §2.2 mandates the directory §5 forgot — a document contradicting itself is a defect, not a conflict between documents
TASK-M1-010 (logic half) | 139998a | gate green, 131 tests; NFD/NFC equivalence now asserted at the ARTIFACT level, not just the inventory — and it failed on first run with FileNotFoundError, exposing that sweep and recon reopened files through the NFC-normalised record path, which does not exist on ext4 when the filesystem stored NFD. FileEntry gained os_path (compare=False, so the record and its equality are unchanged). The bug is invisible on macOS: APFS matches either normalisation on lookup, so the broken code works there and fails only on Linux — the mirror image of the divergence this task was written for. Residue needing a real macOS runner is now exactly one claim: whether APFS hands back NFD for a name written NFC
TASK-M1-010 (Linux half + its security checks) | uncommitted | gate green, 133 tests; ci.yml's `generate artifacts` step run locally with only runner paths changed — two-entry digest over recon.json and hits.jsonl, stable across runs, no NOTHING-COMPARED placeholder, and no absolute path (which would have made the two runners disagree forever for a reason unrelated to determinism); two security properties specific to this task were untested and now are — sweep never reads THROUGH an escaping symlink (the inventory marked one, but following it would quote a file outside the reviewed tree into a match_excerpt), and ordering does not depend on case folding (§4's finding class; a folding sort diverges across platforms independently of NFC). Residue is two filesystem claims no Linux machine can settle, and there is no git remote at all, so CI has never run on this repository
TASK-M1-010 (compare job, H-8) | uncommitted | the compare-platforms job had never been shown to FAIL — it was green having compared nothing once, so "it would catch a real divergence" was an assumption, which is exactly what H-8 says a guard is until someone attacks it. Its comparison step replayed verbatim against three digest pairs: identical → exit 0; one hash differing → exit 1 naming NFR-3; two placeholders → exit 0 while saying it compared nothing. All three correct. Done now rather than after the macOS run, because that run will be the only evidence for a property nothing else can check and a comparison that cannot fail would launder a real divergence into a green tick
NOTES-GUARD | uncommitted | gate green, 150 tests; notes/ holds findings about third-party projects including at least one unfixed, and .gitignore was the entire protection — thin for what it guards, since `git add -f` bypasses it silently and editing the ignore rule for an unrelated reason would pass review. Two assertions, because either alone is defeatable without the other noticing: nothing under notes/ is tracked, and the ignore rule still exists. The asymmetry is the argument — the check costs a millisecond, the failure costs somebody else's exposure, and rewriting history after a push removes the text without unpublishing it. `git` resolved with shutil.which rather than looked up on PATH (S607), matching the care pyproject.toml already records for the harness driver
DISCLOSURE-REDACTION-2 | uncommitted | both gates green; owner reports an UNFIXED finding in the M1 DoD target, and BRIEF_M1.md still named the file inside it and the block the weakness sits in — the coordinates, which is the part that actually matters. Removed from the tree and from the seven commits behind it via filter-branch (BRIEF_M1.md was byte-identical across the range, so one replacement covered it), then refs/original deleted, reflog expired and `git gc --prune=now` run, because filter-branch leaves the originals reachable and a rewrite that leaves the objects behind has redacted nothing. Verified with `git grep` across every commit in `git rev-list --all`: no match for the target's internal path, owner prefix, constant names or advisory id. Goldens checked and clean (they are over tests/fixtures, never the target). Local clone and analysis scripts deleted from the scratchpad. Receipt hashes recorded earlier point at commits before the rewritten range and are unaffected. CONTRIBUTING.md now carries the rule, because this was the second occurrence in one session and the second one is a pattern rather than a slip
DOD-ITEM-10 | uncommitted | target identified from BRIEF_M1.md §3's own entrypoint example rather than guessed; cloned read-only outside the repository, nothing executed; 164 candidates over 30 files, reaching the region the DoD requires. Candidates are unresolved, which is their only correct M1 status — M1 runs Phase 3 and owns no triage. **Location and characterisation deliberately absent**: the owner has an unfixed finding in this target, and naming the file or the shape of the weakness in a repository heading for public is disclosure without the disclosure (G-1, FR-8.4). Earlier versions of this line and of the brief carried both; removed from the working tree and from the history behind it. The methodological point is kept because it is about secrev and not about the target: the catalog reached the region through a coincidence of vocabulary rather than by asking the question that would aim there, that question being one of the two §1 removed to M4 — so a DoD item a coincidence can satisfy is weaker evidence than it looks, which is the argument for D-11's structural source and not a point in the catalog's favour
CI-NEVER-VALIDATED | uncommitted | gate green, 148 tests (was 135); there is no remote, so the three workflow files have never executed and nothing had ever checked them — including the project's own claim that "Every action is pinned to a commit SHA, never a tag", which lived only in CLAUDE.md with nothing enforcing it. A supply-chain control that exists as a sentence is the shape this repository distrusts everywhere else. tests/test_workflows.py now asserts it, plus YAML that parses, `needs:` that resolve, and `scripts/…` references that exist. The claim turned out to be true — 13 assertions pass — which is the point: it is now true *and checked*, so a later `@v4` cannot slip in with no diff to explain it. Includes a guard-the-guard assertion that the workflow list is non-empty, since every other check iterates it and an empty list would pass while checking nothing (H-1)
SAST-FIRST-RUN | uncommitted | CodeQL had never run on this code — no remote, so CI has never executed, and the binary is not installed. Downloaded the pinned bundle (codeql-bundle-v2.26.4) and verified it against the checksum GitHub publishes beside it, the same discipline STACK.md §2.2 applies to gitleaks. Result: **no alerts** over the full Python source, including catalog.py, sweep.py, recon.py, cli.py and inventory.py, none of which had ever been through dataflow analysis. Two failure modes hit on the way and both behaved correctly per H-1 — a missing binary and an unresolvable query pack each exited 2 saying "cannot check", never 0. The pack failure was self-inflicted: the CLI locates its qlpacks relative to its own real path, so symlinking only the executable orphans them; CLAUDE.md now records that
BASH-GUARD-FLAGS | uncommitted | harness gate green, 96 assertions (was 94); found by trying to run the SAST stage — `is_execute` refused a dash-token in ANY position, so `sh scripts/check.sh --fast` and `--sast` were both blocked. Those are the invocations CLAUDE.md documents and the git hooks run, so the guard forbade the project's own workflow, which does not stop the workflow — it moves it where the guard cannot see, and every other refusal in that file loses credibility with it. The rule now checks only the first argument, which is where an interpreter flag has to be: `python3 -c`, `sh -c` and `python3 -m` remain refused (asserted), because no later position turns an interpreter into an evaluator. Cost of the old rule was invisible — nothing failed, the documented command simply could not be run
ID-COLLISION | uncommitted | both gates green, 135 tests; the corrected APFS premise turned out to hide a live defect and not just a wrong sentence. Two files whose names differ only by normalisation both NFC-normalise to one `path`, so they become one record — and with equal content they derive the SAME candidate id, demonstrated before the fix: 2 files, 2 candidates, 1 id. Under P4 resolving one resolves the other, and a verification recorded against one file applies to bytes nobody read — D-6's "a wrong merge silently loses a question" with a verification transferring on top. `inventory.walk` now raises NormalisationCollision (a ValueError, so cli reports exit 2: the target is unreviewable as it stands, not an internal error) naming both on-disk spellings and what to do. Refused rather than disambiguated because APFS cannot hold both variants in one directory, so any scheme telling them apart builds an inventory that exists on Linux and cannot exist on macOS — trading a silent collision for a guaranteed divergence. Two tests: the collision is refused (skipping where the filesystem folds the names, which is the correct outcome there), and a lone NFD name still inventories normally, since refusing those would reject most trees authored on HFS+
APFS-PREMISE | uncommitted | both gates green, goldens regenerated; STACK.md §5 asserted "APFS/HFS+ store NFD, Linux stores NFC" and that is wrong about two of the three — APFS *preserves* the normalisation it is given and achieves insensitivity by hashing the normalised form for lookup, HFS+ was the one that stored a decomposed form, and ext4 is normalisation-agnostic rather than NFC. Checked against Apple's own documentation rather than left as a thing to observe on a runner we do not have. The NFC rule is unchanged and still necessary, for a different reason: decomposed names exist and travel. The sharper divergence was not stated anywhere — APFS refuses to hold two normalisation variants of one name in a directory, so a target containing both has a different *inventory* on macOS, which normalising cannot reconcile. The false premise had been copied into 7 further places (CLAUDE.md, README.md, test_determinism.py, the non-ASCII fixture, and the secrev-invariants, debugging and testing-contract skills) — all corrected, which is H-7's argument for referencing rather than restating, here with a claim that was wrong at the source
M2-UNLOCK | uncommitted | harness gate green, 94 assertions (was 91); closing M1 left the marker at M2 with no rules, so H-6 refused every write to src/ and patterns/ — correct, and the documented remedy is rules, never moving the marker back. BRIEF_M2.md written (scoped from P11, FR-1.3, FR-3.9-3.11, STACK.md §7), carrying TASK-M1-010's macOS run as a DoD box because an obligation that lives only in a commit message stops being one. scope-guard.sh gains an M2 branch: src/ permitted, patterns/ refused (M2 adds a source, not rules — packs are M5), and the surfaces heuristic no longer fires under the milestone that owns surfaces, because a guard objecting to the work it exists to permit teaches people to click through it. Three new assertions cover all of that via the existing scope_at() helper. session-start.sh stopped naming §1/§2/§7 — that is BRIEF_M1's layout and BRIEF_M2's DoD is §4, so it was pointing readers at a section that does not exist
DISCLOSURE-CORRECTION | uncommitted | an earlier version of BRIEF_M1.md and of this file characterised a specific construct in the swept third-party target and pronounced on its soundness. That is a Phase 4 verdict written by a milestone that owns no triage (M1 runs Phase 3; FR-3.2 says patterns are questions), about a third party's live code, in a repository heading for public, with no disclosure step — which is what G-1 and FR-8.4 exist to prevent. Corrected to record what the sweep did rather than what it might mean, and since nothing had been pushed and no remote exists, the three commits carrying it were rewound and recommitted from the corrected tree rather than left in `git log` for a future clone to find. The methodological point is kept because it is about secrev, not about the target
TASK-M1-011 | uncommitted | M1 CLOSED by owner decision, marker M0->M1->M2; BRIEF_M1.md §7 all ten items ticked; attack.py's DoD assertion generalised from "BRIEF_M0.md has no open boxes" to "no brief below the marker has an open box", keyed off the marker so it extends to M3 unaided, and H-8 defeat-verified (marker M2 + one unticked box fails and names it, both restored). Consequence verified rather than assumed: with no M2 rules, scope-guard.sh's `*) refuse_no_rules` now refuses every write to src/ and patterns/ — attempted, and it fired naming H-6. That is the guard saying the project claims a milestone nobody has scoped; the remedy is BRIEF_M2.md plus M2 rules, never moving the marker back. session-start.sh reports M2 and says the brief is absent. TASK-M1-010 stays unticked on purpose and must land in BRIEF_M2.md's DoD, or the marker assertion will let M3 arrive with the macOS run never having happened
TASK-M1-011 (superseded) | uncommitted | harness gate green, 91 assertions (was 90); BRIEF_M1.md §7 fully ticked; attack.py's DoD assertion generalised from "BRIEF_M0.md has no open boxes" to "no brief below the current marker has an open box", keyed off the marker so it extends to M3 without being remembered; H-8 defeat verified — marker set to M2 with one §7 box unticked fails and names the box, both restored. Marker deliberately NOT moved: TASK-M1-010's macOS half is open and closing M1 with that carve-out is an owner decision
SELF-APPLICATION | 139998a | AC-10 / §7 item 9: `sweep src/secrev/` produces **0 hits**. `sweep scripts/` produces 8, of which 4 are high-precision fs.agent_config_write on comment lines mentioning `.claude/` — the rule matches the path rather than a write verb, deliberately (H-2: enumerating write verbs is not a closeable list), so these are candidates a reader resolves, which is what P4 describes. CodeQL could not run locally (needs --codeql <binary>, not installed); it exits 2 rather than 0, which is the H-1 behaviour, and CI runs it on every push
TASK-M2-001 | uncommitted | harness gate green, 98 assertions (was 96); product gate green, 155 tests. `is_nfr3_path` names `src/secrev/surfaces.py` before the file exists, with two assertions (absolute and relative path) that `determinism-guard.sh` speaks on it and names it. H-8 defeat-verified: with the line removed exactly the two new assertions fail, one naming paths.sh as the cause, and the other 96 hold; restored and green. Done before any M2 code because surfaces.py is a third generator of ids in the ledger, and a guard that starts watching after the first write has already missed the one that decided them. The M2 plan went through plan-reviewer first — verdict "conflicts with spec", no scope creep; its one defect was ordering: adding surfaces to the determinism check or the CI digest before Q1 would have let `_emit`'s overwrite drop the pattern ledger out of both NFR-3 comparisons while both stayed green. Owner decisions G0 (Q1 two-block ledger, Q3 no ast in M2, C-1 surface kinds as data outside patterns/, C-2 `decl-20`) are recorded in TASKS_M2.md; the STACK.md §5/§8 amendments C-1 and C-2 imply are raised, not made
TASK-M2-002a | uncommitted | both gates green — 155 tests, 99 guard assertions (was 98), mypy over 9 source files (was 8). `ledger.py` now holds the record every source shares: `Hit`, `to_jsonl`, `window`, `redact`, `excerpt`, `WINDOW_SPEC` from `sweep.py`, and `LAYERS`/`PRECISIONS` from `catalog.py`. Owner decision (Q2): without it the surface source would have imported `Hit` from the pattern source, and a source that can see another source's module is one refactor away from seeing its results — the dependency P11 exists to prevent. A pure move, proven by `tests/golden/hits.jsonl` passing byte for byte and the determinism stage staying green. `ledger.py` decides bytes in every ledger, so it joins `is_nfr3_path`; H-8 defeat-verified (line removed → exactly that assertion fails, naming paths.sh; restored). One transient: determinism-guard.sh fired FAILED mid-edit, because the seven edits landed in parallel and for a moment `cli.py` still imported `WINDOW_SPEC` from `sweep.py`. The full gate run after all edits was green; recorded because a guard firing and then being ignored is the pattern that trains people to ignore it, and this one was checked rather than dismissed
TASK-M2-002b | uncommitted | harness gate green, 104 assertions (was 99). `surfaces/` — where the surface kinds will live, as data per NFR-6 — is protected in the same change that creates it, not after: STACK.md §8 H-4 amended through spec-guard, `bash_guard.py` PROTECTED and its refusal message, `is_scoped_path` in paths.sh, scope-guard.sh's message and its M2 remit comment, and CLAUDE.md's three restatements of the set. Reason recorded in H-4: the kinds decide which entry points enter the ledger at all, so an unreviewed edit narrows the review's scope with nothing reporting it — P11's failure, in the tool's own input. Five assertions, three of them permits or negatives so the guard cannot pass by refusing everything: Bash refuses `tee surfaces/_surfaces.yaml`, permits `cat` of it, and does not protect `build/surfaces.txt` (the regex keys on a directory segment, so `src/secrev/surfaces.py` is not mistaken for the data dir); scope-guard refuses `surfaces/` under M0 and permits it under M2. H-8 defeat-verified: removing both additions failed exactly the two refusals (one reporting rc=0) and left the three permits passing; restored. Known and accepted: `*/surfaces/*` also scopes a directory named `surfaces` anywhere, e.g. under tests/fixtures — the same over-match patterns/ has, failing closed; fixtures must not use that name
TASK-M2-002c | uncommitted | both gates green — 178 tests (was 155), 104 guard assertions, mypy over 10 source files. `surfaces/_surfaces.yaml` holds the surface kinds as data, detection included (`files` globs and a line-oriented `declaration`), so a new reachability class really is a data edit (NFR-6) and not one in name only. `kinds.py` loads it with the catalog's discipline: 23 new tests, most of them refusals that assert the message names the kind. The namespace is closed from both sides — kinds must be `surface.<name>`, and `catalog.py` now refuses a pattern in `surface`, so the two sources cannot put two questions under one `rule_id` in one ledger. `glob_to_regex` moved to `inventory.py` so both sources share one meaning of `**`; proven a pure move by the hits.jsonl golden passing unchanged. First kind is `surface.skill_activation` because C-3 is still open for MCP tools. Q4 is defaulted (the kinds file carries its own `version`), not decided. Not defeat-tested beyond each test asserting its specific message: the loader's refusals are the catalog's shape, whose tests were written red in M1
TASK-M2-003/004/005 | uncommitted | `surfaces.py` exists and the surface source works end to end on the fixture tree. Tests first: `tests/test_surfaces.py` ran red on ModuleNotFoundError before the module was written, then 14 green. AC-9a is asserted in both halves — `sweep` yields nothing for `skills/quiet/SKILL.md` and `surfaces` yields a candidate — so a pattern that later matches the file by coincidence turns the test red instead of passing it. H-8 on the ordering assertion: removing the within-file sort failed exactly that test (`(2, 'surface.alpha')` emitted first) while two-run identity and the golden both stayed green, which is the demonstration that a two-run comparison cannot see an order that is stable but wrong; restored. Goldens regenerated deliberately, never repaired: candidate bytes written to the scratchpad and diffed first — recon changed in exactly the three lines the new fixture explains, the sweep golden not at all. One test defect found and fixed in the test: an unquoted `B?}` is invalid in a YAML flow mapping. `decl-20` added to ledger.py and STACK.md §5 (through spec-guard). `catalog_version` on a surface record is the kinds file's version — the Q4 default, still the owner's call. The first full gate went red twice, both correctly: ruff (an en dash in two docstrings, `os.symlink` over `Path.symlink_to`) and gitleaks, which flagged `generic-api-key` on a credential-shaped value in the redaction test — the exact mistake `test_sweep.py` already records. Fixed at the value (a non-credential-shaped string, the sweep test's own precedent), not by widening `.gitleaks.toml`, whose allowlist says it must not be widened beyond fixtures. Then both gates green: 192 tests, 104 guard assertions, gitleaks clean
TASK-M2-006 (mcp_tool) | uncommitted | both gates green — 206 tests (was 192), 104 guard assertions, determinism stable. `surface.mcp_tool` per C-3: the Python SDK's decorator forms (`@mcp.tool()`, `@server.call_tool()`) and `add_tool(`; a `call_tool(` call site, `list_tools` and a commented-out decorator are not entered. Every shipped kind now has a row in `tests/test_surfaces.py` — a declaration it must enter and a near miss it must not — asserted against the shipped kinds in both directions, so a kind cannot ship without its negative. H-8 on the new rows: with the decorator anchor removed from the declaration, four tests failed (the near miss, the call-site line, the comment line, and the golden, which gained the fixture's own `call_tool(` on line 14) while the `list_tools` and `tools_registry` negatives rightly held; restored and green. Goldens regenerated and diffed in the scratchpad first: `surfaces.jsonl` gains one record; the skill record keeps its id and changes only in `catalog_version` (kinds `2026.09.2`), which is the Q4 default doing what it says; `recon.json` changes in `files_total`, `python` and `loc_total` (+15 = the 14-line fixture and one README row); `hits.jsonl` unchanged, so the fixture triggers no pattern. Extended before commit by owner decision: `surface.mcp_tool_listing` (layer `instruction`, precision `medium`) for a low-level server's `@server.list_tools()`, whose returned descriptions reach the model as written (P8) and which no kind recorded — found by the owner asking why `list_tools` was skipped, which it should not have been silently. Fixture `mcp/lowlevel.py`: two more surface records (the listing on line 9, the dispatcher on line 14), the client's `session.list_tools()` on line 20 not entered; recon +1 file, +21 lines (the 20-line fixture and one README row); `hits.jsonl` unchanged again. Both gates green at 208 tests. H-8 on the listing kind: with the decorator anchor removed, exactly its near-miss row and the golden failed (the golden gaining `session.list_tools()` on line 20 of the fixture); restored and green
TASK-M2-006 (mcp_tool precision) | uncommitted | owner decision after asking what happens when `@x.tool` is not MCP: the kind matches the decorator by name and the import is on another line, so another framework's tool decorator enters, and the record said `precision: high` with a question claiming "any connected client can call it" — MCP's property, not the matched line's. Now `medium`, and the question holds for any client or model that can call the tool; those tools stay in, since a model-callable tool is an entry point whoever declared it. Kinds `2026.09.3`. Golden diffed first: all four ids unchanged, `catalog_version` moved on every record, the two `mcp_tool` records changed in precision and question only; `recon.json` and `hits.jsonl` byte-identical
TASK-M2-006 (mcp_server) | uncommitted | `surface.mcp_server` per C-3: `"command"` (a local server) or `"url"` (a remote one) in `.mcp.json`, `mcp.json` and `claude_desktop_config.json`. `url` goes beyond C-3's literal "the command that starts it", recorded in TASKS_M2.md with the reason: a hosted server has no command. Negatives: `shutdownCommand`, an `--command` argument, a `COMMAND` env key, and `"command"` in `package.json` / `.vscode/tasks.json`. `.mcp.json` fixture confirmed not git-ignored (`check-ignore` exit 1), so it survives a clone. Goldens diffed first: `surfaces.jsonl` gains two records (`command` line 4, `url` line 10; the `--command-timeout` and `COMMAND_LOG` lines not entered), the four existing ids unchanged, `catalog_version` 2026.09.4 on every record; `recon.json` +1 file, `json: 1` in `by_language` and in the coverage-gap line, +14 lines (13-line fixture, one README row); `hits.jsonl` unchanged. First full gate red on ruff alone (format, RUF005, E501 in the new test) with all 221 tests passing; fixed in the test. Then both gates green, 221 tests, 104 guard assertions. H-8: with the declaration loosened to the bare word, exactly the near-miss row (`"args": ["--command"]`) and the golden (the fixture's `--command-timeout` line) failed, while the file-scope test still held, as it should — it tests `files`, not the declaration; restored and green
TASK-M2-006 (hook_binding) | uncommitted | `surface.hook_binding`: `"<Event>": [` in `.claude/settings.json`, `settings.local.json` and a plugin's `hooks/hooks.json`, matched by shape rather than an event-name list so an event added later still enters (asserted with `SomeFutureEvent`). The first regeneration changed the M1 sweep golden: the new fixture-README row spelled the agent config directory's path, the README is part of the fixture tree, and `fs.agent_config_write` matched it at `README.md:21` — the pattern doing its job on text I wrote. Reworded the row rather than accept an M1 golden change for a sentence of documentation, and the row now says why the path is not spelled. Goldens then: `surfaces.jsonl` gains two records (`PreToolUse` line 3, `SessionStart` line 9; `"hooks": [` and `"matcher": "Bash"` not entered), the six existing ids unchanged, `catalog_version` 2026.09.5; `recon.json` +1 file, `json` 1→2, +14 lines (13-line fixture, one README row); `hits.jsonl` unchanged. Fixture confirmed not git-ignored. Both gates green, 239 tests, 104 guard assertions. H-8: with the capital-letter requirement dropped from the declaration, exactly the near-miss row (the lower-case `"hooks": [`) and the golden (the fixture's line 6) failed; restored and green
TASK-M2-006 (cli_command) | uncommitted | `surface.cli_command`: `name = "module.path:function"` in `pyproject.toml`, precision `medium` because a line cannot see its TOML table. Negatives: `build-backend = "setuptools.build_meta"` (no colon), `requires-python`, and URLs (`https:` is followed by `//`, not a name); the same line in `config.toml` is out of the kind's files. Fixture in `cli/`, not the fixture root, so recon's root-only `pyproject.toml` read is untouched — `entrypoints.declared` confirmed unchanged in the diff. Goldens diffed first: `surfaces.jsonl` gains one record (line 8; the `urls`, `requires-python` and `build-backend` lines not entered), eight existing ids unchanged, `catalog_version` 2026.09.6; `recon.json` +1 file, `toml: 1` in `by_language` and in the coverage-gap line, +13 lines (12-line fixture, one README row); `hits.jsonl` unchanged. Fixture not git-ignored; ruff clean on first run. Both gates green, 246 tests, 104 guard assertions. H-8: with the `module:function` value shape removed from the declaration, exactly the near-miss row (three extra records: `build-backend`, `requires-python`, the homepage URL) and the golden (the fixture's other string-valued lines) failed, while the file-scope and declaration-form tests held, as they should; restored and green
TASK-M2-006 (public_export) | uncommitted | `surface.public_export`: `__all__` assigned plainly, annotated, with `+=`, `.extend(` or `.append(`, indentation allowed. Found while writing it: a multi-line `__all__` is not the gap Q3 listed — its first line matches and its names fall in the `decl-20` window — so TASK-M2-008 now names what is actually missed (no `__all__`, names past the window, a dynamically built one). Negatives: `list(__all__)`, `module.__all__`, `__all__ ==`, `self.__all__`, a comment. Goldens diffed first: `surfaces.jsonl` gains one record (line 5; `list(__all__)` on line 11 not entered), nine existing ids unchanged, `catalog_version` 2026.09.7; `recon.json` +1 file, `python` 15→16, +12 lines (11-line fixture, one README row); `hits.jsonl` unchanged. Fixture not git-ignored; ruff clean on first run. Closes TASK-M2-006: seven kinds, each with a `CASES` row and an H-8 defeat. Both gates green, 260 tests, 104 guard assertions. H-8: with the declaration cut to the bare name `__all__`, seven tests failed — the near-miss row, all five non-declaration lines (reading, `module.__all__`, `==`, `self.__all__`, a comment), and the golden (the fixture's `list(__all__)` on line 11); restored and green
TASK-M2-006b | uncommitted | owner's approach, after I raised the gap at `hook_binding`: a kind's `files` now match without regard to case (`re.IGNORECASE` on each compiled glob, in `kinds.py`), because a case-insensitive filesystem hands the agent `.Claude/settings.json` for `.claude/settings.json` and the kinds matched names exactly. Held apart by tests: the record keeps the on-disk spelling (the id derives from it); a declaration stays case-sensitive (`"Command"`, `DESCRIPTION:` not entered); and the shared `glob_to_regex` stays case-sensitive, since `paths_exclude` uses it and ignoring case there fails open. Kinds `2026.09.8`. Goldens diffed first: every surface record changed in `catalog_version` only, no record added, ids unchanged; `recon.json` and `hits.jsonl` byte-identical — the fixture tree holds no oddly-cased file, so the change shows only in the new tests. The Bash guard refused direct `ruff`/`mypy` runs naming `src/`; the full gate runs both. Both gates green, 268 tests, 104 guard assertions. H-8: with `re.IGNORECASE` removed, exactly the six case tests failed (five spellings in `test_surfaces.py`, one in `test_kinds.py`) while the two boundary tests — declaration case-sensitive, shared glob case-sensitive — held; restored and green. `python-reviewer`: nothing blocking; two points taken — recompiling from `.pattern` would have dropped any flag `glob_to_regex` gains later (splitting one glob into two meanings between kinds and `paths_exclude`), so the glob's flags are now kept and `IGNORECASE` added on top; and `IGNORECASE` folds some non-ASCII letters (`ſ`→s, Kelvin→k), which only adds candidates and is now pinned by a test and the docstring. Writing that pin, ruff flagged the literal long s twice (RUF002 in the docstring, RUF001 in the test) — the ambiguous-character rule doing its job on exactly the confusable it exists for; the docstring now names code points and the test uses `\N{LATIN SMALL LETTER LONG S}`, so the source stays ASCII and says which character it means
TASK-M2-007 | uncommitted | `secrev surfaces` and the two-block ledger (Q1), with the owner's two decisions taken before code: stdout is the run's own block only, and `run.json` keeps one entry per command. Tests first: 12 new in `tests/test_cli.py`, plus the `run.json` test updated for the new shape — red on `invalid choice: 'surfaces'` (13 failing, 11 passing) before `cli.py` changed. `surfaces` never loads the catalog (asserted by making `resolve_catalog` raise) and takes `--kinds` instead of `--catalog`; a malformed kinds file exits 2 naming the kind. `merge_ledger` keeps other blocks' lines byte for byte and writes them in `SOURCES` order, so `sweep`→`surfaces` and `surfaces`→`sweep` give one file, each block equal to its source's golden, a rerun changes nothing; a workspace ledger with a line it cannot read as its own (not JSON, not an object, a foreign `source`) is refused with exit 2 before anything is written, and left as it was. `run.json`'s hard-coded `"recon"` for `sweep` is gone with the per-command shape; the surface entry carries `kinds_version` and `decl-20`, not a catalog version. Gate green, 280 tests (was 268). `python-reviewer` found a real defect: `merge_ledger` split with `str.splitlines`, which also breaks on U+0085/U+2028/U+2029, and `to_jsonl` writes those unescaped — so a target file named with one cut its own record in two and every later run was refused, the reviewed target locking the surface source out of its workspace (P11). Fixed by splitting on `\n` alone and never re-splitting the run's own block; also fixed from the same review: a ledger with no final newline is refused rather than joined onto, a non-UTF-8 ledger is refused naming the file, a dropped `run.json` says so on stderr, stdout moved after `run.json`, and any unexpected exception is exit 3, never the interpreter's 1 (a bug must not read as a gate verdict). Each has a test; 288 tests. H-8 twice: with `splitlines` restored, exactly the three separator tests and the end-to-end filename-lock test failed (the latter reproducing the review: `surfaces` 0, then `sweep` 2); with the run's block appended last instead of placed in `SOURCES` order, six failed — command order, golden concatenation, rerun, and the three separator tests — and `test_both_blocks_are_kept_pattern_block_first` PASSED, because it ran `sweep` first, the one order in which appending lands the pattern block first by luck. Reordered to run `surfaces` first, then run against the still-broken join: it now fails (surface block first), then the join was restored. The same review's broader finding — `sweep.py` and `surfaces.py` split content with `splitlines` too, shifting line numbers after a form feed — is recorded in TASKS_M2.md for the owner, not fixed here
LINES | uncommitted | owner decision to fix the review's broader finding now: `sweep.py`, `surfaces.py` and `recon.py`'s `loc_total` split text with `str.splitlines`, which also breaks on U+000B, U+000C (form feed, `^L`), U+001C to U+001E, U+0085, U+2028 and U+2029, so every line number and window after one ran past the editor's count while staying deterministic — NFR-3 could not see it. One `inventory.split_lines` (CRLF, CR, LF only; terminators discarded, so windows stay LF-joined and `autocrlf` re-identifies nothing) now serves all three; the `sweep.py` docstring that argued for `splitlines` is corrected rather than deleted. `tests/test_lines.py`, 41 tests: the helper, agreement with `splitlines` wherever that was right, and each of the eight characters in each consumer against the editor's line. Red first against the two modules not yet changed: 16 failures, exactly the eight pattern-line and eight line-count cases. `surfaces.py` had been changed before the red run, so H-8 on it instead: `splitlines` restored there failed exactly the eight surface-line cases, with every `test_surfaces.py` test still passing — the new file is the only guard; restored. Gate green, 329 tests (was 288), and every golden unchanged: no fixture contains any of the characters (checked with grep before the change, confirmed by the goldens after)
TASK-M2-008 | uncommitted | `recon.json`'s `coverage_gaps` stops saying the surface source is unimplemented (BRIEF_M2.md §4) and says what it cannot reach: a line naming the code languages present that no kind reads (the structural line's shape — "read in Python only; not read for: shell, typescript", or "no other code language present"), then HTTP routes and IPC handlers (the DoD's two), the parser beneath a CLI command, a package with no `__all__`, a runtime-built `__all__` and names past its window, declarations split across lines or several on one, hooks outside Claude Code settings and plugin hooks, and CLI commands in setup.cfg/setup.py/package.json — each limit the owner accepted on the condition that it is named. Fixed text in recon.py rather than derived from the kinds: recon does not import the surface source (P11). Tests first: 12 red before the change (the stale-line test, nine named gaps, two language tests). Golden diffed first: only `coverage_gaps` changed, the stale line replaced by nine; `surfaces.jsonl` and `hits.jsonl` untouched. The old line was quoted nowhere else in the repository. First gate red on `ruff format` alone (two gap strings split across lines that fit on one), 340 tests passing; joined. Then both gates green, 340 tests (was 329), 104 guard assertions. H-8, two defects at once in separate regions: with the HTTP-routes line removed and `python` no longer excluded from the unread languages, exactly four failed — the golden, the `HTTP routes` gap, and both language tests; restored
TASK-M2-009 | uncommitted | the determinism stage and CI's Linux/macOS digest now cover both blocks of the ledger, after TASK-M2-007 made that safe (before it, `surfaces` would have overwritten the pattern block and both comparisons would have dropped it while staying green — the ordering defect plan-reviewer caught). `determinism_check.py`: the two-run check runs `recon`, `sweep`, `surfaces` and fails when either block is absent ("that block was never compared", H-1); the stable-id check runs both sources; `_ids` now splits records on newline alone, the same `splitlines` flaw the TASK-M2-007 review found, which would have crashed the check on a record holding U+0085. `ci.yml`: `surfaces` runs before the digest. Gate green, 340 tests; the stage now prints "recon.json and both hits.jsonl blocks byte-identical, ids stable". H-8: with `surfaces` dropped from the two-run check, the stage exits 1 naming the missing surface block; restored. First gate runs red on ruff alone (the new message line, 105 then 102 characters); shortened. **Not verified:** the cross-platform half runs only on GitHub, and the owner is holding pushes for compute minutes — the macOS digest over both blocks is unproven until then
TASK-M2-010 (pattern half + SAST) | uncommitted | at `3708d10`. `--sast`: CodeQL **no alerts**, every other stage green (340 tests). `secrev sweep` over the whole repository (the Bash guard refuses a command naming `src/` beside an interpreter flag, so the root was swept and filtered by path): 484 records. `src/secrev/`: **2**, both `fs.agent_config_write` on `kinds.py:130-131` — the TASK-M2-006b docstring naming `.claude/settings.json` as its example; the rule matches the path rather than a write verb on purpose (H-2), and prose writes nothing, so each resolves in a sentence. M1's run found 0; these arrived with that docstring. `scripts/`: 8, the same count as M1 — 4 `fs.agent_config_write` on comments naming `.claude/`, 4 low-precision `path.traversal` on joins inside the scripts' own `tempfile` directories. Also swept: `src/secrev.egg-info/PKG-INFO`, a pip build artifact carrying a README copy; the inventory excludes no `*.egg-info`, but `.gitignore:14` does, so it exists only on a machine that ran `pip install -e .` and never in a clone. The surface half could not be run from this shell: the guard reads the subcommand name `surfaces` as the protected directory — the branch-name finding's second instance, recorded in TASKS_M2.md
GUARD-HONESTY | uncommitted | two reviews of that over-match landed on the same point: the Bash guard is a guardrail against mistakes, not a control — its trigger is a test over path spellings, which `cd src && …`, `rm -rf sur*aces`, `d=surf; rm -rf "${d}aces"`, `find . -delete` and `python -c` all defeat. The file already said so in its KNOWN LIMIT; the outward documents did not. Done: the refusal now names the token that matched and says it may be a false positive of a spelling test (verified live on `python3 -m secrev surfaces`); `STACK.md` §8 H-2 and `CLAUDE.md` say guardrail rather than "the Bash bypass is closed"; three new assertions (107, was 104) — the message names the token, `rm -rf src` and `mv surfaces old` are still refused (what the trailing `$` buys, and why requiring `/` would be a regression rather than the fix I first proposed), and the KNOWN LIMIT section must stay in the file. **A live hole closed on the way:** `.claude/settings.json` allowed `Bash(find:*)` while the guard deliberately leaves `find` out for carrying `-delete` and `-exec`; `find . -name '*.py' -delete` names no protected token and was permitted by both layers. Removed. Added `ask` rules on `Write|Edit|MultiEdit` to `.claude/hooks/**` and `.claude/settings.json`, so editing the mechanism needs the owner's explicit approval — enforced by the tool layer, not by reading shell text, which is the review's strongest point: if the agent can edit the file defining the hook, every other control collapses. Rejected and recorded: structural shell parsing, renaming the product's subcommand, requiring a trailing `/`. Carried beyond M2 in TASKS_M2.md with full plans: HARNESS-CI (a CI check on protected paths plus CODEOWNERS, which runs where the agent does not) and HARNESS-FS (root ownership and `chattr +i` on the hooks, with its repair workflow, its costs, and why it gets no assertion in `.claude/check.sh`)
