# M0 receipts

One line per completed task: id | commit | what was actually asserted.

TASK-000 | fcd52ee | commits=1, worktree clean, gate exit 0 (no test file — baseline snapshot, no behaviour to test)
TASK-001 | dc46a54 | 16/16 green on unmodified harness; 2 fail + exit 1 when a guard block is defeated; guard restored byte-identical
TASK-002 | 67b8d94 | 18/18 green; malformed payload now refuses (was rc=5, undefined); exit 1 when the new H-1 path is defeated; guard restored byte-identical
TASK-002B | b94156e | 19/19 green; gate reports the stage; defeating a guard gives GATE exit 1 (was 0); guard restored byte-identical
TASK-003 | 65241ae | 34/34 green; brief §1 heredoc case refused with reason; cat/grep/head/git-diff of same path permitted; no false positives on transcripts|descriptions|mysrc
TASK-007 | 88d3ed0 | 42/42 green; 0 hooks invoke jq (6 mention it in comments); scope+spec malformed payload now exits 2 (was 5); defeating either H-1 path turns the gate red
TASK-008 | 0e222d6 | 47/47 green; eval into scripts/self_check.py blocked; patterns/ correctly exempt from self-application, covered by scope; removing scripts/ from paths.sh turns the gate red
TASK-009 | bfbaef4 | 52/52 green; relative src/secrev and scripts paths now guarded; over-match measured (transcripts, descripts, foosrc all refused; mypatterns and docs not); re-anchoring turns the gate red
TASK-010 | 3b48c25 | 57/57 green; M9+src exits 2, M9+README untouched, missing MILESTONE exits 2, M1 unchanged; both the refusal and the ordering fail the gate when defeated
TASK-011 | 74404f1 | 62/62 green; session-start reports M0 + BRIEF_M0.md; M0 permits scripts/ refuses src+patterns; M9 and missing marker still refuse; M0 fall-through turns the gate red
TASK-012 | f9e3882 | 64/64 green; no live agent restates STACK.md; removed copy was stale on 3 counts (claimed mypy as a recorded dep, predated §8 H-1..H-8, predated the jq decision); restating one line turns the gate red
TASK-013 | 3a779cd | 68/68 green; CLAUDE.md status table + hook table current, README hook count mechanical (11 == 11); 8 stale claims asserted absent; re-adding one, or adding a 12th hook, turns the gate red
TASK-014 | c6a9414 | 69/69 green; §8 now H-1..H-9; mypy in §2; uv no longer default and 3 remedy sites updated + asserted; one assertion retired because TASK-014 made its pinned claim true
TASK-005 | c460af4 | 72/72 green; gate now runs ruff+format+pytest for real (69 collected); 15 lint findings fixed not suppressed; pyproject dev deps unreachable by pip until moved; restoring the PATH fallback turns the gate red
TASK-009B | 3f2fe31 | 74/74 green; transcripts/descripts/foosrc no longer refused, relative+absolute still are; found and fixed 2 `cmd | head` status losses (async-check reported [ok] under a failing pytest; determinism-guard would have announced byte-identical over a failure)
TASK-004 | e528258 | 81/81 green; sh/python3 <script> permitted, bash and extension mismatch refused, -c refused; ; && || | newline substitution all refuse beside a protected path; read-only pipeline now refused (asserted cost); relaxing OPERATOR_CHARS turns the gate red
TASK-004B | 0800648 | 85/85 green; sed -i/rm/redirect into .claude refused, cat/grep/ls/sh of it permitted, Write untouched; removing .claude from PROTECTED turns the gate red (defeat verified by asserted anchor after a first attempt silently no-opped)
TASK-004C | 64f5e97 | 85/85 green; BRIEF §1 verification run LIVE through the Bash tool — heredoc to src/secrev/cli.py refused, sed -i refused; cost measured immediately (gate output can no longer be piped through grep); docs said "not wired" in 3 separate places
TASK-006 | a997d46 | 87/87 green; with .venv/bin/python moved aside the gate exits 2 naming ruff (same state gave exit 0 + "all gates pass" before); pytest branch no longer conflates "no tests" with "no pytest"; remedy string corrected — TASK-014's `pip install -e '.[dev]'` fails until src/ exists
TASK-012B | 060938a | 87/87 green; stack copies removed from disabled/planner.md and architect.md, both already stale (mypy as a recorded dep, no §8); H-7 assertion now covers disabled/; closes open question 6
TASK-016 | d937646 | 88/88 green; MILESTONE M0 -> M1, session-start reports M1 and finds BRIEF_M1.md; a new assertion refuses to let the marker move while any BRIEF_M0.md DoD box is unticked

# M1 receipts

Same form. "gate" is `sh scripts/check.sh` with every stage live; "harness" is `sh .claude/check.sh`.
Commit hashes are filled in at commit time; `uncommitted` means the work is on disk and gated but
not yet history.

PUBLICATION | 902f3bf | gate green; LICENSE (PolyForm Noncommercial 1.0.0, verbatim from upstream tag 1.0.0 + Required Notice), SECURITY.md, README.md, CONTRIBUTING.md, STACK.md §10; pyproject gains license/readme/authors — wheel build verified `License: PolyForm-Noncommercial-1.0.0` at metadata 2.4 with LICENSE packaged, and `pip install -e .` (the path CI's install-paths job asserts) still works; license_check.py's SELF comment and its printed line both claimed the project "carries none", now false and corrected
TASK-M1-001..004 | e053c26, 6c5879d | (recorded retroactively) determinism stage keyed on inventory.py; test_determinism.py red before the module; inventory.py + fixture tree; ids.py deriving from (relative_path, rule_id, window_sha256, ordinal) — the ledger's own acceptance text still named the superseded `line` formula and was corrected rather than ticked as written
DECISIONS-M1 | 9f3c4ed | gate + harness green; four owner decisions recorded with reasoning, not just applied — window_spec on candidate AND verification records plus a new FR-4.6 invalidation row (STACK.md §5, PRD FR-4.5/4.6/§7); default_severity_hint (PRD beat the brief on precedence); multi-element `layer` refused in M1 rather than silently flattened; path.traversal added as a ninth seed pattern with an explicit note that it does not reverse §1's M4 exclusion
TASK-M1-005 | 139998a | gate green, 22 catalog tests; unknown keys, bad flags, uncompilable regex, non-namespaced id, duplicate id, missing version, multi-layer all exit with the pattern id named; `multiline` refused by name with the reason; the safe-loader test was strengthened after review — it asserted only that a CatalogError was raised, which an unsafe loader would also have produced (the tag evaluates to 0 and the schema check then rejects it), so it passed against precisely the defect it was written to catch; now asserts the refusal happens at the YAML layer
TASK-M1-005B | 139998a | gate green; `python -m secrev sweep --help` now contains `--catalog`, so ci.yml's catalog job runs its assertion instead of taking the ::warning:: branch and exiting 0 having asserted nothing (H-1 in the job that exists to prove exit 2); verified live: malformed catalog exits 2 with `ci.deliberately_broken` on stderr, which is where that job reads it from
TASK-M1-006 | 139998a | gate green, 25 pattern tests; nine patterns, positive+negative fixture each, pairing asserted in BOTH directions so an added pattern fails and a renamed one leaves no orphan; the negative fixture earned its place immediately — net.bind_all was a bare `0\.0\.0\.0`, which matches inside `10.0.0.0/8`, caught before the rule had run against anything real; owasp omitted from 8 of 9 rather than guessed (§4: omit rather than force a bad fit); fixtures live in tests/fixtures/rules/ not .../patterns/, because the Bash guard's unanchored glob (H-5) reads the latter as the protected catalog
TASK-M1-007 | 139998a | gate green, 28 sweep tests; golden byte-comparison (35 records); two runs identical; adding a file ahead of another changes no existing id; an edit >20 lines above a match does not re-identify it; CRLF and LF produce the same ids while line numbers stay against the original; D-6 held for two rules on one line and two identical matches on one line; the non-ASCII fixture matched nothing so it never reached the golden — the §9 requirement was satisfied on paper only, and the fixture now carries a candidate; G-3 redaction asserted, and the test's own `ghp_`-shaped literal was flagged by gitleaks 4 times and replaced with a neutral one rather than allowlisted
TASK-M1-008 | 139998a | gate green, 15 recon tests + golden; determinism stage's "waiting on recon.py, sweep.py" is gone and the artifact half now runs — recon.json and hits.jsonl byte-identical, ids stable; no subprocess anywhere in the codebase, git identity read from .git/ directly (loose refs then packed-refs), degrading to ("directory", None) rather than guessing a version that would split one target's history across two workspace paths; the slug test found a real defect (café slugged to `caf-`) after being rewritten to build NFC/NFD explicitly instead of comparing a literal to itself
TASK-M1-009 | 139998a | gate green, 15 CLI tests; exit 0/2/3 asserted apart, and sweep finding 35 candidates still exits 0 because exit 1 means "unresolved remain" and in M1 every candidate is unresolved by definition; stdout parses as JSONL with the workspace line on stderr; G-4 refusal verified live and asserted, including that no directory is created; run.json is the only artifact carrying a timestamp; the workspace file and stdout are byte-identical
TASK-M1-012 | 139998a | gate green; STACK.md §5's exclusion list extended with .venv-audit and the tool caches (exact names, not a `.venv*`/`*_cache` pattern — P3); recon of this repository goes from 1952 files / 703,763 lines / 1416 Python files to 137 / 16,348 / 38; resolved rather than raised because §5 and §2.2 are both in STACK.md and §2.2 mandates the directory §5 forgot — a document contradicting itself is a defect, not a conflict between documents
TASK-M1-010 (logic half) | 139998a | gate green, 131 tests; NFD/NFC equivalence now asserted at the ARTIFACT level, not just the inventory — and it failed on first run with FileNotFoundError, exposing that sweep and recon reopened files through the NFC-normalised record path, which does not exist on ext4 when the filesystem stored NFD. FileEntry gained os_path (compare=False, so the record and its equality are unchanged). The bug is invisible on macOS: APFS matches either normalisation on lookup, so the broken code works there and fails only on Linux — the mirror image of the divergence this task was written for. Residue needing a real macOS runner is now exactly one claim: whether APFS hands back NFD for a name written NFC
TASK-M1-010 (Linux half + its security checks) | uncommitted | gate green, 133 tests; ci.yml's `generate artifacts` step run locally with only runner paths changed — two-entry digest over recon.json and hits.jsonl, stable across runs, no NOTHING-COMPARED placeholder, and no absolute path (which would have made the two runners disagree forever for a reason unrelated to determinism); two security properties specific to this task were untested and now are — sweep never reads THROUGH an escaping symlink (the inventory marked one, but following it would quote a file outside the reviewed tree into a match_excerpt), and ordering does not depend on case folding (§4's finding class; a folding sort diverges across platforms independently of NFC). Residue is two filesystem claims no Linux machine can settle, and there is no git remote at all, so CI has never run on this repository
TASK-M1-010 (compare job, H-8) | uncommitted | the compare-platforms job had never been shown to FAIL — it was green having compared nothing once, so "it would catch a real divergence" was an assumption, which is exactly what H-8 says a guard is until someone attacks it. Its comparison step replayed verbatim against three digest pairs: identical → exit 0; one hash differing → exit 1 naming NFR-3; two placeholders → exit 0 while saying it compared nothing. All three correct. Done now rather than after the macOS run, because that run will be the only evidence for a property nothing else can check and a comparison that cannot fail would launder a real divergence into a green tick
NOTES-GUARD | uncommitted | gate green, 150 tests; notes/ holds findings about third-party projects including at least one unfixed, and .gitignore was the entire protection — thin for what it guards, since `git add -f` bypasses it silently and editing the ignore rule for an unrelated reason would pass review. Two assertions, because either alone is defeatable without the other noticing: nothing under notes/ is tracked, and the ignore rule still exists. The asymmetry is the argument — the check costs a millisecond, the failure costs somebody else's exposure, and rewriting history after a push removes the text without unpublishing it. `git` resolved with shutil.which rather than looked up on PATH (S607), matching the care pyproject.toml already records for the harness driver
DISCLOSURE-REDACTION-2 | uncommitted | both gates green; owner reports an UNFIXED finding in the M1 DoD target, and BRIEF_M1.md still named the file inside it and the block the weakness sits in — the coordinates, which is the part that actually matters. Removed from the tree and from the seven commits behind it via filter-branch (BRIEF_M1.md was byte-identical across the range, so one replacement covered it), then refs/original deleted, reflog expired and `git gc --prune=now` run, because filter-branch leaves the originals reachable and a rewrite that leaves the objects behind has redacted nothing. Verified with `git grep` across every commit in `git rev-list --all`: no match for the target's internal path, owner prefix, constant names or advisory id. Goldens checked and clean (they are over tests/fixtures, never the target). Local clone and analysis scripts deleted from the scratchpad. Receipt hashes recorded earlier point at commits before the rewritten range and are unaffected. CONTRIBUTING.md now carries the rule, because this was the second occurrence in one session and the second one is a pattern rather than a slip
DOD-ITEM-10 | uncommitted | target identified from BRIEF_M1.md §3's own entrypoint example rather than guessed; cloned read-only outside the repository, nothing executed; 164 candidates over 30 files, reaching the region the DoD requires. Candidates are unresolved, which is their only correct M1 status — M1 runs Phase 3 and owns no triage. **Location and characterisation deliberately absent**: the owner has an unfixed finding in this target, and naming the file or the shape of the weakness in a repository heading for public is disclosure without the disclosure (G-1, FR-8.4). Earlier versions of this line and of the brief carried both; removed from the working tree and from the history behind it. The methodological point is kept because it is about secrev and not about the target: the catalog reached the region through a coincidence of vocabulary rather than by asking the question that would aim there, that question being one of the two §1 removed to M4 — so a DoD item a coincidence can satisfy is weaker evidence than it looks, which is the argument for D-11's structural source and not a point in the catalog's favour
CI-NEVER-VALIDATED | uncommitted | gate green, 148 tests (was 135); there is no remote, so the three workflow files have never executed and nothing had ever checked them — including the project's own claim that "Every action is pinned to a commit SHA, never a tag", which lived only in CLAUDE.md with nothing enforcing it. A supply-chain control that exists as a sentence is the shape this repository distrusts everywhere else. tests/test_workflows.py now asserts it, plus YAML that parses, `needs:` that resolve, and `scripts/…` references that exist. The claim turned out to be true — 13 assertions pass — which is the point: it is now true *and checked*, so a later `@v4` cannot slip in with no diff to explain it. Includes a guard-the-guard assertion that the workflow list is non-empty, since every other check iterates it and an empty list would pass while checking nothing (H-1)
SAST-FIRST-RUN | uncommitted | CodeQL had never run on this code — no remote, so CI has never executed, and the binary is not installed. Downloaded the pinned bundle (codeql-bundle-v2.26.4) and verified it against the checksum GitHub publishes beside it, the same discipline STACK.md §2.2 applies to gitleaks. Result: **no alerts** over the full Python source, including catalog.py, sweep.py, recon.py, cli.py and inventory.py, none of which had ever been through dataflow analysis. Two failure modes hit on the way and both behaved correctly per H-1 — a missing binary and an unresolvable query pack each exited 2 saying "cannot check", never 0. The pack failure was self-inflicted: the CLI locates its qlpacks relative to its own real path, so symlinking only the executable orphans them; CLAUDE.md now records that
BASH-GUARD-FLAGS | uncommitted | harness gate green, 96 assertions (was 94); found by trying to run the SAST stage — `is_execute` refused a dash-token in ANY position, so `sh scripts/check.sh --fast` and `--sast` were both blocked. Those are the invocations CLAUDE.md documents and the git hooks run, so the guard forbade the project's own workflow, which does not stop the workflow — it moves it where the guard cannot see, and every other refusal in that file loses credibility with it. The rule now checks only the first argument, which is where an interpreter flag has to be: `python3 -c`, `sh -c` and `python3 -m` remain refused (asserted), because no later position turns an interpreter into an evaluator. Cost of the old rule was invisible — nothing failed, the documented command simply could not be run
ID-COLLISION | uncommitted | both gates green, 135 tests; the corrected APFS premise turned out to hide a live defect and not just a wrong sentence. Two files whose names differ only by normalisation both NFC-normalise to one `path`, so they become one record — and with equal content they derive the SAME candidate id, demonstrated before the fix: 2 files, 2 candidates, 1 id. Under P4 resolving one resolves the other, and a verification recorded against one file applies to bytes nobody read — D-6's "a wrong merge silently loses a question" with a verification transferring on top. `inventory.walk` now raises NormalisationCollision (a ValueError, so cli reports exit 2: the target is unreviewable as it stands, not an internal error) naming both on-disk spellings and what to do. Refused rather than disambiguated because APFS cannot hold both variants in one directory, so any scheme telling them apart builds an inventory that exists on Linux and cannot exist on macOS — trading a silent collision for a guaranteed divergence. Two tests: the collision is refused (skipping where the filesystem folds the names, which is the correct outcome there), and a lone NFD name still inventories normally, since refusing those would reject most trees authored on HFS+
APFS-PREMISE | uncommitted | both gates green, goldens regenerated; STACK.md §5 asserted "APFS/HFS+ store NFD, Linux stores NFC" and that is wrong about two of the three — APFS *preserves* the normalisation it is given and achieves insensitivity by hashing the normalised form for lookup, HFS+ was the one that stored a decomposed form, and ext4 is normalisation-agnostic rather than NFC. Checked against Apple's own documentation rather than left as a thing to observe on a runner we do not have. The NFC rule is unchanged and still necessary, for a different reason: decomposed names exist and travel. The sharper divergence was not stated anywhere — APFS refuses to hold two normalisation variants of one name in a directory, so a target containing both has a different *inventory* on macOS, which normalising cannot reconcile. The false premise had been copied into 7 further places (CLAUDE.md, README.md, test_determinism.py, the non-ASCII fixture, and the secrev-invariants, debugging and testing-contract skills) — all corrected, which is H-7's argument for referencing rather than restating, here with a claim that was wrong at the source
M2-UNLOCK | uncommitted | harness gate green, 94 assertions (was 91); closing M1 left the marker at M2 with no rules, so H-6 refused every write to src/ and patterns/ — correct, and the documented remedy is rules, never moving the marker back. BRIEF_M2.md written (scoped from P11, FR-1.3, FR-3.9-3.11, STACK.md §7), carrying TASK-M1-010's macOS run as a DoD box because an obligation that lives only in a commit message stops being one. scope-guard.sh gains an M2 branch: src/ permitted, patterns/ refused (M2 adds a source, not rules — packs are M5), and the surfaces heuristic no longer fires under the milestone that owns surfaces, because a guard objecting to the work it exists to permit teaches people to click through it. Three new assertions cover all of that via the existing scope_at() helper. session-start.sh stopped naming §1/§2/§7 — that is BRIEF_M1's layout and BRIEF_M2's DoD is §4, so it was pointing readers at a section that does not exist
DISCLOSURE-CORRECTION | uncommitted | an earlier version of BRIEF_M1.md and of this file characterised a specific construct in the swept third-party target and pronounced on its soundness. That is a Phase 4 verdict written by a milestone that owns no triage (M1 runs Phase 3; FR-3.2 says patterns are questions), about a third party's live code, in a repository heading for public, with no disclosure step — which is what G-1 and FR-8.4 exist to prevent. Corrected to record what the sweep did rather than what it might mean, and since nothing had been pushed and no remote exists, the three commits carrying it were rewound and recommitted from the corrected tree rather than left in `git log` for a future clone to find. The methodological point is kept because it is about secrev, not about the target
TASK-M1-011 | uncommitted | M1 CLOSED by owner decision, marker M0->M1->M2; BRIEF_M1.md §7 all ten items ticked; attack.py's DoD assertion generalised from "BRIEF_M0.md has no open boxes" to "no brief below the marker has an open box", keyed off the marker so it extends to M3 unaided, and H-8 defeat-verified (marker M2 + one unticked box fails and names it, both restored). Consequence verified rather than assumed: with no M2 rules, scope-guard.sh's `*) refuse_no_rules` now refuses every write to src/ and patterns/ — attempted, and it fired naming H-6. That is the guard saying the project claims a milestone nobody has scoped; the remedy is BRIEF_M2.md plus M2 rules, never moving the marker back. session-start.sh reports M2 and says the brief is absent. TASK-M1-010 stays unticked on purpose and must land in BRIEF_M2.md's DoD, or the marker assertion will let M3 arrive with the macOS run never having happened
TASK-M1-011 (superseded) | uncommitted | harness gate green, 91 assertions (was 90); BRIEF_M1.md §7 fully ticked; attack.py's DoD assertion generalised from "BRIEF_M0.md has no open boxes" to "no brief below the current marker has an open box", keyed off the marker so it extends to M3 without being remembered; H-8 defeat verified — marker set to M2 with one §7 box unticked fails and names the box, both restored. Marker deliberately NOT moved: TASK-M1-010's macOS half is open and closing M1 with that carve-out is an owner decision
SELF-APPLICATION | 139998a | AC-10 / §7 item 9: `sweep src/secrev/` produces **0 hits**. `sweep scripts/` produces 8, of which 4 are high-precision fs.agent_config_write on comment lines mentioning `.claude/` — the rule matches the path rather than a write verb, deliberately (H-2: enumerating write verbs is not a closeable list), so these are candidates a reader resolves, which is what P4 describes. CodeQL could not run locally (needs --codeql <binary>, not installed); it exits 2 rather than 0, which is the H-1 behaviour, and CI runs it on every push
TASK-M2-001 | uncommitted | harness gate green, 98 assertions (was 96); product gate green, 155 tests. `is_nfr3_path` names `src/secrev/surfaces.py` before the file exists, with two assertions (absolute and relative path) that `determinism-guard.sh` speaks on it and names it. H-8 defeat-verified: with the line removed exactly the two new assertions fail, one naming paths.sh as the cause, and the other 96 hold; restored and green. Done before any M2 code because surfaces.py is a third generator of ids in the ledger, and a guard that starts watching after the first write has already missed the one that decided them. The M2 plan went through plan-reviewer first — verdict "conflicts with spec", no scope creep; its one defect was ordering: adding surfaces to the determinism check or the CI digest before Q1 would have let `_emit`'s overwrite drop the pattern ledger out of both NFR-3 comparisons while both stayed green. Owner decisions G0 (Q1 two-block ledger, Q3 no ast in M2, C-1 surface kinds as data outside patterns/, C-2 `decl-20`) are recorded in TASKS_M2.md; the STACK.md §5/§8 amendments C-1 and C-2 imply are raised, not made
