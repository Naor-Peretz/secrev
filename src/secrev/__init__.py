"""secrev — security review of agentic artifacts.

The package is deliberately empty of behaviour. Everything lives in the
modules, and `cli.py` is the only entry point (STACK.md §3).
"""

__all__: list[str] = []
